#include "main.h"

/*----Bule LED----PB9-----*/


void Led_Configuration(void)
{
	/* 
    GPIO_InitTypeDef gpio;
    
	  RCC_AHB1PeriphClockCmd(,ENABLE);
	
		gpio.GPIO_Pin =  ;
		gpio.GPIO_Mode = ;
		gpio.GPIO_OType = ;
		gpio.GPIO_Speed = ;
		GPIO_Init(,&gpio);
    */
}
